// ==================== Submitted: 2026-08-09T15:20:22.301Z [java] ====================
class Solution {
    public int maxLengthBetweenEqualCharacters(String s) {
        int[] firstIndex = new int[26];
        java.util.Arrays.fill(firstIndex, -1);
        
        int maxLength = -1;
        
        for (int j = 0; j < s.length(); j++) {
            int charIdx = s.charAt(j) - 'a';
            
            if (firstIndex[charIdx] == -1) {
               
                firstIndex[charIdx] = j;
            } else {
                
                int currentLen = j - firstIndex[charIdx] - 1;
                maxLength = Math.max(maxLength, currentLen);
            }
        }
        
        return maxLength;
    }
}
